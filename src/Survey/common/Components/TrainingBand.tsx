import { Trans as T } from 'react-i18next';

const TrainingBand = () => (
  <div className="bg-black p-1 text-center text-sm font-bold text-white">
    <T>Training Mode</T>
  </div>
);

export default TrainingBand;
